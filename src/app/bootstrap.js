(function() {
  const tool = window.EngineeringTool = window.EngineeringTool || {};
  tool.app = tool.app || {};
  tool.catalog = tool.catalog || {};
  tool.domain = tool.domain || {};
  tool.engine = tool.engine || {};
  tool.connect = tool.connect || {};
  tool.scene = tool.scene || {};
  tool.editor = tool.editor || {};
  tool.math = tool.math || {};
})();